import { BaseComponentInterface } from "@/types";

const Header: BaseComponentInterface = {
    init() {
        console.log("header initialized");
    },

    destroy() {},
};

export default Header;
